{
  "name": "smartcontract",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia",
    "deploy:all": "hardhat run scripts/deploy-all.js --network sepolia",
    "register:restaurants": "hardhat run scripts/register-restaurants.js --network sepolia",
    "verify": "hardhat verify --network sepolia",
    "export:abi": "node scripts/export-abi.js",
    "clean": "hardhat clean",
    "node": "hardhat node"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.0.0",
    "@openzeppelin/contracts": "^5.3.0",
    "dotenv": "^16.5.0",
    "hardhat": "^2.25.0"
  }
}
